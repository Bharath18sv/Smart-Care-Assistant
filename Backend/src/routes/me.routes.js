// import { asyncHandler } from "../utils/asyncHandler";
// import { ApiError } from "../utils/apiError.js";
// import { Doctor } from "../models/doctor.models";
// import { Patient } from "../models/patient.models.js";
// import { ApiResponse } from "../utils/apiResponse.js";


// export default asyncHandler(async (req, res) => {
//   // This route is for user-specific operations, like fetching user details
//   // You can implement the logic here to fetch the user details from the database
//   const {email} = req.user; // Assuming user details are stored in req.user after authentication
//   if (!email) {
//     return res.status(400).json({ message: "User not authenticated" });
//   } 
  
//   const user = await 

//   res.status(200).json({ message: "User details fetched successfully" });
// });
